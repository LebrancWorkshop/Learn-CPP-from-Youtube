// Get all input to the array. 
// Sort input.
// Chech that the first index (index = 0) is 0 or not
// if the first index is 0, just swap the values from the first and the second index.
// if not just don't do anything more.
// distribute the value of the array to be the decimal integer number; 

#include <iostream>

int sort(int* data)
{
	return 0;
}

int main()
{
	int size;
	std::cin >> size;

	int data[size];
	for(int i = 1; i <= size; i++)
	{
		std::cin >> data[i];
	}
	return 0;
}